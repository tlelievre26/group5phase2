import { MetricsDataRetriever } from "./phase1/services/metrics-data-retriever"

//Functions for uploading a package

function parseUrlToZip(url: String) {
    //Ideally I'd just steal some of the code from the phase 1 and put it into its own function/file but idk if that's allowed
    //const repo_fields = MetricsDataRetriever.extractGitHubInfo(url);
}